

################# TODO #################
## Creer des entités membres et user(gerant) - tables
    # Utiliser la table User en pour etendre les fonctionnalités gerant
## inclure le fait q'un gerant que seul les user ont un mot de passe
## Nommer un user consiste à dupliquer les informations d'un membre et lui ajouter les informations de logins

## Un gerant gère un seul groupe
## Gerer le fait qu'on ait differents types de gerant ou rôle pour un gerant(adminGroupe, AdminVille, amdinSection)
## 



## ville-section-gropue

    if (countries) {
        countries.addEventListener('change', performFilter);
        sections.addEventListener('change', performFilter);
        groups.addEventListener('change', performFilter);
    }
    if (sections) {
        sections.addEventListener('change', performFilter);
        groups.addEventListener('change', performFilter);
    }
    if (groups) {
        groups.addEventListener('change', performFilter);
    }


            if(ville){
            xhr.open('GET', '{{ route('filter') }}?ville=' + ville.value + '&section=' + section.value + '&groupe=' + groupe.value, true);
            console.log("heloooooooooooooooooVille"+ville.value);

        }else if(section){
            xhr.open('GET', '{{ route('filter') }}?section=' + section.value + '&groupe=' + groupe.value, true);
            console.log("heloooooooooooooooooSection"+section.value);

        }else if(groupe){
            xhr.open('GET', '{{ route('filter') }}?groupe=' + groupe.value, true);
            console.log("heloooooooooooooooooGroupe"+groupe.value);
        }
        
        xhr.send();
